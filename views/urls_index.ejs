<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>

</head>
<body>
    <header>
        <% include partials/_header %>
    </header>
    <!-- Add your EJS code here -->
  <h2>List of URLs</h2> <a href='/urls/new'>Create New Short URL</a>
  <ul>
      <% for (short in urls[userID]) { %>
        <li style="padding:3px"><%= short %> &rarr; <a href=<%=urls[userID][short]%>><%=urls[userID][short] %></a>
          <a style="padding-left:5px" href="/urls/<%=short%>">Edit</a>
          <form method="post" action="/urls/<%=short%>/delete" style="display:inline-block; padding-left:10px" >
            <button style="padding:5px">Delete &#x1F5D1;</button>
          </form>
        </li>
      <% } %>
  </ul>
</body>
</html>